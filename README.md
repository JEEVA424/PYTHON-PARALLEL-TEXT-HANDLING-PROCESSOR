# Python Parallel Text Handling Processor

This project processes large text datasets using rule-based sentiment analysis and provides a Streamlit UI dashboard.

Features:
- File Upload
- Parallel Text Processing
- Sentiment Analysis
- Results Dashboard
- Search Function
- CSV Export

Run the project:

pip install streamlit pandas
python -m streamlit run app.py
